#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../aws-shell/lib/aupload" "$@"
  ret=$?
else 
  node  "$basedir/../aws-shell/lib/aupload" "$@"
  ret=$?
fi
exit $ret
